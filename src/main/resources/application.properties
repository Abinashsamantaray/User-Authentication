# Server port
server.port=8080

# MySQL database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/userauthentication
spring.datasource.username=root
spring.datasource.password=abinash

# Optional: Specify the driver class if needed (Spring Boot auto-detects this for MySQL 8.x+)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA properties (Hibernate)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true 
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect


# Mail configuration (use your SMTP settings)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=abinashsamantaray01@gmail.com
spring.mail.password=avbt iadd srua usno
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Cache configuration
spring.cache.type=simple

# Thymeleaf template prefix
spring.thymeleaf.prefix=classpath:/templates/
e